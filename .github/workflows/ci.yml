name: Eclectic

on:
  pull_request:
  push:

jobs:
  test:
    name: Test
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
      - name: Check Out
        uses: actions/checkout@v4
      - name: Build And Test
        run: |
          sudo apt install valgrind
          sudo gem install ceedling
          pip install gcovr
          chmod +x ceedling
          rm -fr Build
          ./ceedling
        working-directory: ./Project/Ceedling/
      - name: Test Report
        if: always()
        uses: mikepenz/action-junit-report@v4
        with:
          include_passed: true
          report_paths: ./Project/Ceedling/Build/artifacts/gcov/junit_tests_report.xml
      - name: Coverage
        uses: 5monkeys/cobertura-action@master
        with:
          minimum_coverage: 90
          path: ./Project/Ceedling/Build/artifacts/gcov/gcovr/GcovCoverageCobertura.xml
          skip_covered: false
